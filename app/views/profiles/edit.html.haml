.container 
  %ul
    - @profile.errors.full_messages.each do |message|
      %li= message
  
  = form_with(model: @profile, url: profile_path, method: 'put', local: true, class: 'form-long') do |f|
    .field
      = f.label :nickname, 'Nickname'
      %br/
      = f.text_field :nickname, class: 'text'
    .field
      = f.label :introduction, 'Introduction'
      %br/
      = f.text_area :introduction
    .field
      = f.label :gender, 'Gender'
      %br/
      = f.select :gender, Profile.genders.keys, {}, { class: 'text'}
    .field
      = f.label :birthday, 'Birthday'
      %br/
      = f.date_field :birthday, class: 'text'
    .field
      = f.check_box :subscribed
      = f.label :subscribed, 'Receive notifications'
    
    = f.submit 'Submit', class: 'btn-primary'