.task.card
  .card_header
    = link_to board_task_path(task.board_id, task.id) do
      .card_title
        = task.name
  .card_content
    = task.description
  .card_detail
    = image_tag task.user.avatar_image